// LICENSE : MIT
"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.default = mergeMatches;
function mergeMatches() {
    var results = [];

    for (var _len = arguments.length, aMatches = Array(_len), _key = 0; _key < _len; _key++) {
        aMatches[_key] = arguments[_key];
    }

    aMatches.forEach(function (matches) {
        matches.forEach(function (targetMatch) {
            var alreadyHave = results.some(function (match) {
                var text = match.text,
                    index = match.index;

                return targetMatch.index === index && targetMatch.text === text;
            });
            if (!alreadyHave) {
                results.push(targetMatch);
            }
        });
    });
    return results;
}
module.exports = exports['default'];
//# sourceMappingURL=merge-matches.js.map